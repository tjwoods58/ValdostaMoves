<script type="text/javascript">
    $(function () {
        $("[data-toggle='tooltip']").tooltip();
    });
</script>

<div id= "forum_align">
<h1>Listing recipes</h1>

<table class="table table-bordered">
  <thead>
    <tr>
      <th>Recipe Image</th>	
      <th>Name</th>
      <th>Added By</th>
      <th>Ingredients</th>
      <th>Instructions</th>
      <th></th>
      
    </tr>
  </thead>

  <tbody>
    <% @recipes.each do |recipe| %>
      <tr>
      	<td style="width:200px;"><%= image_tag(recipe.imagesrc, class: 'list_image') %></td>
        <td style="width:150px;"><strong><%= recipe.name %></strong></td>
        <td><%= recipe.user.email %></td>
        <td><%= recipe.description %></td>
        <td><%= recipe.instructions %></td>
        <td><%= link_to 'Show', category_recipe_path(@category, recipe), class: 'btn btn-info' %>
        
          <% if can? :update, Recipe %>
            <%= link_to 'Edit', edit_category_recipe_path(@category, recipe), class: 'btn btn-primary' %>
          <% end %>
        
        
          <% if can? :destroy, Recipe %>
            <%= link_to 'Destroy', category_recipe_path(@category, recipe), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger' %>
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<% if can? :create, Recipe %>
  <%= link_to 'New Recipe', new_category_recipe_path(@category) %>
<% end %>
|<%= link_to ' Back', categories_path %>


</div>
<div id= "forum_align">

<%= will_paginate @recipes %>

</div>
